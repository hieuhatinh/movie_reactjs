const routes = {
    home: '/',
    profile: '/profile',
    setting: '/setting',
    history: '/history',
    search: '/search',
    login: '/login',
    register: '/register',
};

export default routes;
